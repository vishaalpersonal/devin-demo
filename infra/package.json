{
  "name": "ops-console-infra",
  "private": true,
  "description": "Mock Pulumi program (not deployed). Run `pnpm install` here before `pulumi preview`.",
  "main": "index.ts",
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
